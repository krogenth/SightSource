export interface Tour {
    id: number
    countryId: number
    tour: string
    description: string
    price: number
    duration: string
    thumbnail: string
    images: string[]
    popular:boolean
    dayNum:number
}